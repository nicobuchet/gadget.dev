---
name: debug-typescript
description: >
  Use when encountering TypeScript errors, type mismatches, TS2345/TS2322 errors,
  generic constraint issues, or when the build fails due to type errors. Triggers
  on "type error", "TS error", "not assignable to", "build failed".
metadata:
  author: portable
  version: "1.0.0"
---

# TypeScript Error Debugging Runbook

Systematic approach to resolving TypeScript errors.

Follow `runbook.md`. Read the FULL error before attempting any fix.
